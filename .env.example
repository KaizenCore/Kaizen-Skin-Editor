# Kaizen Skin Editor Environment Configuration
#
# Vite automatically loads environment files based on mode:
# - .env                 # loaded in all cases
# - .env.development     # loaded in dev mode (npm run dev)
# - .env.production      # loaded in prod mode (npm run build)
#
# Access mode in code: import.meta.env.MODE ('development' or 'production')
# Access dev check: import.meta.env.DEV (true/false)
# Access prod check: import.meta.env.PROD (true/false)

# Kaizen Core OAuth Configuration
VITE_KAIZEN_CLIENT_ID=your_client_id_here
VITE_KAIZEN_BASE_URL=https://kaizencore.tech
VITE_KAIZEN_REDIRECT_URI=http://localhost:1420/oauth/callback

# Kaizen Skin API
# Dev: http://localhost:8000/api/v1
# Prod: https://skin-api.kaizencore.tech/api/v1
VITE_SKIN_API_URL=http://localhost:8000/api/v1
